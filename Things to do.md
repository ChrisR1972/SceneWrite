* Reference image prompt in identity block should be editable by the user with a save button to save changes.
* In the identity block when the reference image prompt has been generated the Generate Reference Image Prompt button changes to Regenerate Image Prompt.
* when the final conclusion is generated and the character outlines are generated the window shows the character outlines. Change this so the final conclusion is visible otherwise the user doesn't see the final conclusion unless they scroll up.
* Place all dialogue within " ".
* Reinforce that objects the characters don't directly interact with should not be placed within \[ ].
* Instead of the save scene changes button let's change this to an 'Approve' button and align this button to the left, once this has been approved then the entities should be extracted to the identity blocks and not before.
* In the generated scene content add to the text edit menu, on right mouse click, the option to classify a word ready for extraction. These classifications are environment, character, vehicle and object. Also in this menu add a selection for action words and SFX so they can be classified to be used in the storyboard. Add the appropriate markup to the text. If a word already has a classification it can either be de-classified or changed to a different classification by selecting that classification. This operation is when the user's mouse is hovering over the word they want to classify, just the same as if a word is spelt wrong and the user wants suggestions for correct spelling.
* Action words need to have a markup so the ai can define them in the storyboard. Let's use \* \* for example; \*jump\*
* In screenplay writing once an entity has been described in the scene content the ai must not repeat the description in other scene unless there has been a change to that entity in the story.
* When the user clicks on save in the top menu, change the window name to 'Save Story' and pre-fill the filename to the name of the story.
* If the user clicks new story and confirms they want to do this when a previous story is open and not saved this should be cleared straight away.
* vehicles markup should be single curly brackets { }
* The sun beats down on the sprawling expanse of the launchpad - launchpad not given markup \_Launchpad\_
* The air is filled with the hum of machinery - hum not given markup
